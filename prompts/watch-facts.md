アイドルマスター関連の定点観測で見つかった新着ページ(末尾。`### 番号. URL` ごとに1件)から、事実を抽出して JSON 配列で返します。
JSON 配列だけを出力し、ほかの文字は書きません。

## 1. 手順
- 本文が「取得できず」のページは、URL を WebFetch で読む
- まとめサイトのページは、ページ内の一次ソースの URL を `url` にする

## 2. 規則
{RULES}

## 3. 出力の形
{ITEM}

## 新着ページ
{MATERIAL}
