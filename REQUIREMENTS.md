# アイマスNEWS(α) システム要件書

作成日: 2026-07-11 ／ 対象: 新規構築(日刊AI新聞)
配信: 2026-08-23 に GitHub Pages から**自前オリジン(Caddy)+ Cloudflare Tunnel** へ移行(PIPELINE.md §9.6)。GitHub Pages はフォールバックとして残す。

## 1. コンセプト

- アイドルマスター関連ニュースを**毎日自動発行**する非公式ファン新聞。紙名「アイマスNEWS(α)」。
- 全記事を AI 編集部が執筆し、別モデルの AI が校閲する。人間は校閲しない。この体制を隠さず、免責と透明性を紙のアイデンティティとする(紙面フッター・about に常時明記)。
- コアバリュー:
  1. 「今日アイマスで何があったか」が30秒で掴める(ダイジェストが紙面の目次)
  2. 全号を発行当時の紙面のまま永久保存する(**append-only**)。訂正も当時の表示で残る
- デザインは確定済みモック(トップ/記事/社説/バックナンバー)を正とする。本書はそのデザインが要求するデータ契約とパイプラインを定義する。

## 2. 紙面仕様(確定デザインより)

### 2.1 発行体裁

- 日刊。**発行時刻 06:00 JST**。号数は創刊号を第1号とする通し番号(第NNN号 / NO.0000 表記)。稼働前の試験発行は全て**第0号**とする(number: 0。連番 lint の対象外。main に載せた試験号も append-only で残す)。
- レスポンシブ3モード: SP(<600px)/TB(<1024px)/PC。モバイルファースト。
- ダイジェストの配置はモード別: SP=格納バー+**1画面固定・スクロールなしの全画面オーバーレイ**、TB=紙面冒頭に常設、PC=右カラム常設。
- 画像素材は一切使用しない。タイポグラフィ(Shippori Mincho B1/Zen Kaku Gothic New/IBM Plex Mono)・罫線・色のみで構成する。

### 2.2 ページ構成

| ページ | パス | 内容 |
|--------|------|------|
| トップ(最新号) | `/` | 号ヘッダー/面ナビ(推しフィルタ)/ダイジェスト/リード記事/large×2・medium・small記事/社説抜粋/24時間ランキング/きょうの誕生日/バックナンバー導線 |
| 号ページ | `/editions/YYYY-MM-DD/` | 過去号をトップと同一レイアウトで完全再現。トップは実質「最新号の別名」 |
| 記事 | `/articles/YYYY-MM-DD-<slug>/` | バッジ・ブランド帯・見出し・リード(左罫)・本文(中見出し可)・出典ボックス(必須)・タグ・同じ面の記事・前後記事ナビ・訂正表示。URL に日付を含め、slug の一意性は号内のみ要求する(永続的な名前空間の衝突を防ぐ) |
| 社説面 | `/editorials/YYYY-MM-DD/` | 中央揃えの読み物レイアウト+「これまでの社説」(社説も全号保存) |
| バックナンバー | `/archive/` | 月ジャンプ+号リスト(号数・日付・件数・訂正数・一面見出し) |
| about・編集方針と免責 | `/about/` | AI編集部の体制、訂正方針、非公式表明 |

### 2.3 推しフィルタ

トップの面ナビは11チップ(すべて+ブランド10種)。タップで紙面内の記事カード・ダイジェスト行をクライアントサイドでフィルタする。JS はこのフィルタと SP ダイジェスト開閉のみに限定する。

### 2.4 ブランドとカラートークン

| id | 表示名 | カラー |
|----|--------|--------|
| general | 総合 | #6b7280 |
| 765 | 765AS | #e94e6f |
| cg | シンデレラ | #2681c8 |
| million | ミリオン | #f3c012 |
| shiny | シャニ | #8fb3de |
| sidem | SideM | #10ae87 |
| gaku | 学マス | #e8a824 |
| dsva | 876・ヴイアラ | #b06fc9 |
| joint | 合同 | #7d6f5c |
| other | その他 | #9aa0a6 |

基調色: 地 #fbfaf7/墨 #1c1b18/訂正 #b0483f ほかモックの通り。CSS 変数としてデザイントークン化する。

### 2.5 ソース種別(5種)と視覚言語

| 種別 | バッジ様式 | 意味 |
|------|-----------|------|
| 公式 | 黒地白抜き | **アイマス公式**(公式ポータル・ブランド/作品の公式サイト・公式Xアカウント)に到達済み。作品公式サイトはドメインを問わない(例: バンナムAM配下のツアマス公式サイトは公式)。他はどれほど信頼できても公式を名乗らない |
| 準公式 | 薄地黒枠 | 公式レーベル・公式ストア等のグループ/パートナー企業(日本コロムビア・ランティス・アソビストア・アソビノート等) |
| 当事者 | 濃グレー枠 | 主催者・販売元・自治体・コラボ先など、上記以外の当事者による一次発信(例: ふるさと納税の商品ページ) |
| 報道 | 黒枠 | 独立した報道メディアが出典 |
| ファン | グレー枠 | ファン・サークル活動等の当事者発信 |
| もちより | 破線枠 | コミュニティ持ち寄り由来(将来面) |
| 未確認 | 黄破線枠 | 一次ソース未到達。本文は断定を避ける |

### 2.6 訂正の視覚言語

- 公開後の記事は**削除しない**。誤りは記事冒頭の「おわびと訂正」ボックス(初出→訂正後、訂正日時、契機)+本文中の訂正マーク(初出表記の明示)で残す。
- バックナンバー一覧にも号ごとの訂正数を表示する。訂正は恥ではなく透明性の証、という思想を UI が体現する。

## 3. データ契約

デザインが参照する全データを以下の2種で供給する。ビルド(Jekyll)はこの契約のみに依存する。

### 3.1 記事 frontmatter(`_posts/YYYY-MM-DD-<slug>.md`)

```yaml
slug: sample-article          # 号内で一意。URL は /articles/<edition>-<slug>/ になる
edition: 2026-08-01           # 所属号(=発行日)
brand: sidem                  # 2.4 の enum
src: 報道                     # 2.5 の enum
rank: large                   # lead | large | medium | small | roundup(lead は号内で1本 / roundup はブランド内1本)
title: "見出し(折返し前提・上限45字)"
lede: "リード文(60〜100字)"
tags: [タグA, タグB]
sources:                      # 1件以上必須
  - type: 公式
    label: "出典元表記「告知タイトル」(2026年7月31日)"
    url: https://...
corrected: false
corrections: []               # {at, by, initial, corrected_to, note}
event_date: 2026-07-31        # 出来事の発生日(任意)
---
本文(Markdown。中見出しは h2)
```

### 3.2 号スナップショット(`_editions/YYYY-MM-DD.md` — frontmatter のみ)

GitHub Pages 標準 Jekyll で `/editions/YYYY-MM-DD/` に URL 出力するため、スナップショットは collection 文書(frontmatter のみの .md)とする。内容は以下の YAML がそのまま frontmatter になる。

```yaml
number: 12                    # 通し号数(キー名 no は YAML の boolean 解釈と衝突するため不採用)
date: 2026-08-01
weekday: 金
issued_at: "2026-08-01T06:00:00+09:00"
pages: 6                      # 記事が存在する面の数(機械算出)
article_count: 18             # 機械算出
corrected_count: 0            # 機械算出(corrected: true の記事数)
lead_slug: sample-article     # 一面リード記事
digest:                       # 「本日の紙面」。4群固定
  - label: 本日
    rows:
      - k: 誕                 # 種別1文字(語彙は 5.4 で定義)
        t: "行タイトル(20字以内)"
        d: "補足(25字以内)"
        brand: cg
        slug: sample-article  # タップ遷移先(任意)
  - label: 昨日
  - label: 継続中
  - label: 明日
ranking:                      # 8件固定
  - { n: 1, name: アイドル名, brand: cg, delta: up }   # up|down|stay|new
birthdays:
  - { name: アイドル名, brand: cg, note: ひとこと }
```

### 3.2b 社説(`_editorials/YYYY-MM-DD.md`)

社説は号スナップショットに埋め込まず、独立した collection 文書として `/editorials/YYYY-MM-DD/` に出力する(重複保持による不整合を避けるため)。トップの社説欄は同日の社説文書の title / excerpt を参照する。

```yaml
edition: 2026-08-01
title: 社説見出し
excerpt: "紙面掲出用の冒頭抜粋(80字)"
corrected: false
corrections: []               # 記事と同じ訂正契約
---
社説本文(Markdown)
```

**append-only 原則**: compose は当日の `_editions/` と `_posts/` を新規作成するのみ。過去号のファイルには訂正(corrections 追記と該当箇所修正、corrected_count 加算)以外で触れない。機械算出フィールドはスクリプトが記事群から導出し、LLM は手書きしない。

### 3.3 派生データの算出(スクリプト)

- **アイドル名鑑 `_data/idols.json`**: 用意済みの誕生日マスタ JSON を採用する。name・brand・birthday を基本とし、(1) 「きょうの誕生日」欄、(2) ランキングの名寄せ辞書、の両方の基盤とする。
- **24時間ランキング**: スクリプトが決定論的に算出し、LLM は手書きしない。スコア = 当日の記事(見出し・lede・本文)とダイジェストにおける名鑑登録アイドルの登場回数 + 当日 candidates のうち探索(Grok/X 動向)由来エントリでの言及回数(トレンド分)。上位8名を採用し、delta は前号との比較で up/down/stay/new を付与。同点は前号順位を優先し、次いで名鑑順。紙面分とトレンド分の重み付けは第0号(試験発行)で実測して確定する。

## 4. パイプライン

```
collect(コード・毎日数回) → verify(コード) → compose(LLM・発行日早朝)
 → lint(コード・required check) → review(校閲AI) → squash merge → deploy(自前オリジンへ配信)
```

実行主体はローカルマシンのスケジューラ(`claude`・`grok`・`codex` CLI のセッション認証がローカルにあるため)。GitHub 側は Pages 配信と push 後の lint CI(事後検査)のみで、マージ判定はローカルの lint+校閲で行い main へ直接 push する。号ごとに `edition/YYYY-MM-DD` ブランチを main から作成し、収集・生成コミットはすべて同ブランチに載せ、発行時にローカルで **squash merge**(main は1号=1コミット)→ ブランチ削除 → 翌日ブランチ作成、とする。残存する edition ブランチが発行忘れの検知装置を兼ねる。運用詳細は [PIPELINE.md](PIPELINE.md)。

### 4.1 collect

- **定点観測**: `sources.yml` に宣言した公式ポータル・各ブランド公式ニュース・主要メディアの RSS/ページ差分を巡回。
- **探索**: Claude の Web 検索(`claude -p`・ブランド別クエリセット)+ Grok(`grok -p`・X 動向)。探索由来は `verify: unconfirmed` で登録。
- 結果は `candidates/YYYY-MM-DD.json` に正規化して蓄積(id・title・brand・source_type・url・found_at・event_date・dedup_key・facts・origin)。candidates は次号のための調査データであり当該サイクルの2ファイルしか読まれない。未来の予定は `stock/scheduled/<発火日>.json` へ素材スナップショット付きで予約し、話題単位の既報管理は `stock/stories.yml` へ。
- ローカル cron で実行。1日4回+発行日 03:30 の締切前スイープ。

### 4.2 verify

- 候補ごとに一次ソース URL をフェッチし、日付・内容の一致を照合。結果を confirmed / unconfirmed / failed でタグ付け。
- **X(Twitter)由来の情報は Grok の観測をもって verify とする**。X はログイン必須で機械的な再検証ができないため、「Grok が観測した公式ポスト」をそういうソースとして信頼して扱う(公式アカウントの告知は「公式」バッジ)。
- **一次ソースに書かれていない事実は記事に追記しない**ことをパイプライン全体の絶対規則とする。

### 4.3 compose

- 発行日早朝(04:00 目安)に Claude Code のヘッドレス実行(ローカル cron)が起動。締切は**当日 04:00 時点の candidates**。
- 検証済み candidates+stock から、記事群・号スナップショット・社説・ダイジェストを生成し、`edition/YYYY-MM-DD` ブランチにコミットする。
- 生成基準日 = 発行日。「本日/昨日/明日」は発行日基準でのみ書く(生成日と発行日は常に一致)。

### 4.4 lint

publish がマージ前にローカルで実行し、**赤なら発行不可**とするゲート。push 後も GitHub Actions で常時検査し、すり抜けを事後検知する。

- frontmatter・_editions のスキーマ検証(enum・必須・一意性・**lead が号内1本=欠落はブロック**)
- 記事本数の下限(8本)割れの検出(警告。publish が Discord 通知)
- 時制 lint: 本文・lede・digest 内の相対表現(本日/昨日/明日)と絶対日付の共起を edition 日付と照合
- 出典 lint: sources 1件以上、URL 生存(HTTP 200)、candidates に存在しない URL の検知
- ダイジェスト lint: 4群構成、行タイトル20字・補足25字、**SP 1画面制約(1群4行まで・全体12行まで。モック実測由来の暫定値)**
- ランキング8件・delta 語彙・スクリプト算出値(3.3)との一致、append-only 違反(過去号ファイルの diff)検知
- 見出し上限45字(警告)

### 4.5 review(校閲AI)→ squash merge

- 執筆とは**別ベンダーのモデル**が固定チェックリストで校閲する(執筆=Claude/校閲=Codex・モデル terra)。
- **ブロック**: 出典にない事実/URL 捏造/新事実のない続報記事(5章8)/個人への攻撃・プライバシー侵害/(もちより面稼働後)掲載NG投稿の混入。**コメントのみ**: 表記ゆれ・文字数超過・面白さの過不足。
- 指摘→修正コミット→再レビューを最大2往復。lint green+校閲 approve で squash merge し、06:00 の発行に間に合わせる。
- 2往復で未解決・締切超過・candidates 0件などの異常時は Discord webhook で運営に通知し、人間判断(または休刊)とする。

### 4.6 訂正フロー(発行後)

誤り検知(読者指摘・後日の再検証)→ `correction/<slug>` ブランチで訂正(corrections 追記+本文修正+corrected_count 加算)→ lint+校閲 → main へマージ。紙面ファイルの削除は lint が拒否する。

## 5. 編集規程(生成プロンプトの仕様)

1. 一次ソース至上主義: 出典に書かれていない事実を書かない。未確認情報は「未確認」バッジ+非断定の文体で隔離する。
2. 公式一次情報は事実(日付・価格・締切等)を必要なだけ詳述してよい。報道メディア由来は表現を借りず事実のみを抽出し、要約は元記事の代替にならない分量に留めて出典へ誘導する。
3. 画像・映像・音声は掲載しない(リンクのみ)。公式ロゴ・ビジュアルの模倣をしない。
4. 対象範囲: アイドルマスター関連の公式発表・報道。声優個人のアイマス外活動・関係者の動向は対象外。**同人イベント・ファン主催企画(オンリーイベント・同人誌即売会・非公式コラボ等)は当事者ではなく、その告知・締切・開催を記事化しない**。ファン発の話題は、全国トレンド入り級の「現象」として扱える場合のみ「ファン」種別で慎重に扱う。個人の SNS 投稿は単体で記事化しない。
5. 炎上・係争は、読んだ人が嫌な気分になるものは載せない。載せる場合も事実のみ。
6. 社説は毎日1本、その日の紙面から1題。AI 編集部という自己言及を織り込んでよい。
7. 文体・見出し・各欄の字数は本書のデータ契約の上限に従う。
8. **続報規程(毎日必ず回る制度)**: 続報はネタ切れ時の埋め草ではなく、**初報を書いた時点で未来の続報が予約される制度**とする。`stock/scheduled/<発火日>.json` に予約(トリガー日=**締切3日前・締切当日・開幕当日・千秋楽/終了日・発売日・結果発表日**。素材スナップショット同梱)し、compose は毎朝、発行日のファイルだけを機械的に受け取り、**必ず記事化候補として処理する**。締切接近は読者に最も実利がある記事であり、「本日から」は立派な新報、結果は初報の回収である。任意の続報(新事実の発生)は従来どおり `stock/stories.yml` の既報 facts と照合し、新事実がある場合のみ記事化。新事実もトリガーもない話題はダイジェスト「継続中」行のみで扱う。
9. **分量基準(本文のみ)**: lead 800〜1200字/large 500〜800字/medium 300〜500字/small 150〜250字/roundup 200〜700字(規程13の定常運営まとめ)。逸脱は lint 警告。報道メディア由来のみをソースとする記事は上限を1ランク下の値に抑える(規程2)。分量が足りない話題は膨らませず rank を下げる。
10. **内規と紙面の区別**: 本書や PIPELINE の規則・運用値(「全記事に必須」「毎日1本」等)を紙面に表記しない。読者向けの文言は読者に意味のある情報のみとする。
11. **記事本数規程**: **記事化基準(規程4・5・8)を満たす話題は、すべて記事にする。「今日は多いから落とす」という取捨選択の作業は存在しない** — 紙面は無制限である。10〜14本は「最低限そこを目指す」目安であって上限ではない。話題があふれた日は rank を small に寄せて全部載せる(rank 調整は紙面バランスのためで、ドロップの代替ではない)。下限 **8本**割れは lint 警告+Discord 通知の上で発行を止めない(下限をブロックにすると休刊に倒れ、日刊の信頼を損なう方が痛い)。lead 欠落はブロック(一面のない新聞はあり得ない)。下限を守る主たる手段が規程8の続報キューである。
12. **鮮度・初出規程**: 記事にできるのは「発行日時点で新しく発表された・起きる・起きた」ことだけである。収集・執筆の各段で出典の**掲載日(公開日・投稿日)とイベントの年**を必ず確認し、過年度の告知・終了済みの施策を新報として扱わない(第0号期に2025年の生配信告知を「本日開催」と報じた事故に由来)。「発表された」「判明した」等の新規性表現は初出が直近であると確認できた場合のみ使い、初出が過去日なら「(M月D日付で)発表されている」と初出日を明示する。**初出日・掲載日が確認できない情報に新規性の演出をしない**。過去の話題は規程8の続報条件(新事実 or 当日トリガー)を満たす場合のみ扱える。
13. **1記事1主題**: 1本の記事は1つの話題を報じる。複数の小ネタを「まとめ」「続々判明」等として1記事に束ねない(定常運営情報の寄せ集めはニュースではない)。

    **例外(定常運営まとめ = rank: roundup)**: 上の原則は「ニュースを束ねるな」であって「載せるな」ではない。単独では記事にならない**定常運営情報に限り**、ブランドごとに号内1本の `rank: roundup` へまとめてよい。

    - 対象: 開催中のガシャ・ログインボーナス・楽曲追加・月例イベント・配信出演など、**発表済みで進行中**の運営情報
    - **対象外(必ず単独記事にする)**: 発表・開催決定・販売開始日・受注/申込の開始と締切・中止延期。これらはニュースであり、roundup へ流すことは規程11違反(取捨選択の隠れ蓑)にあたる
    - 3件以上まとまる面だけ作る。1〜2件なら通常の small 記事にする
    - roundup は規程11の下限8本に**算入しない**(まとめで下限を満たしたことにすると単独記事の不足が隠れるため)
    - 各項目には出典を持たせる。roundup であっても規程12(鮮度・初出)は同じく適用する

    この例外は、ダイジェストが「その号の記事への索引」(4群・計12行・slug 参照)として実装されており、記事にならなかった話題の受け皿として機能しないために設けた。ダイジェストは索引のままとする。

14. **期間ラベル規程**: 期間を書くときは、**何の期間かを出典の語で明示する**。チケット・受注では次が別々の期間として併存し、取り違えると「誰がいつ買えるのか」が逆になる。

    | 期間 | 対象 |
    |------|------|
    | 先行抽選の申込受付 | 誰でも申し込める(買えたとは限らない) |
    | 抽選結果発表 | — |
    | **入金** | **当選者のみ** |
    | 一般先着販売 | 誰でも(先着順) |
    | 一般販売 | 誰でも |

    - **収集**: `facts` に期間を書くときは原文の見出し・語句をそのまま先頭に付ける(「入金期間: 8/26〜8/30」)。ラベルを外して言い換えない。**原文にラベルの無い日付範囲は facts に書かない**(何の期間か分からないまま渡すと、執筆側が意味を補ってしまう)
    - **執筆**: 出典で確かめずに「販売期間」「発売」「受付」へ一般化しない。先行と一般が両方ある話題では、どちらの話かを毎回明示する
    - **校閲**: 記事の期間ラベルが出典の facts と一致するかを照合する(ブロック項目11)

    2026-08-26号「上水流宇宙BDライブ、現地LVチケット販売開始」で、出典の期間を「販売期間 8/26〜8/30」として収集し、当選者向けの期間を「今から誰でも買える期間」として報じた事故に由来する。**収集の時点でラベルが失われると執筆側では復元できない**ため、規程は収集層から適用する。

### 5.4 ダイジェスト種別語彙(k の1文字)

`誕`(誕生日)`開`(開幕・開始)`終`(終了・千秋楽)`配`(配信)`売`(発売)`受`(受注・受付)`締`(締切)`発`(発表)`演`(出演・イベント)を初期語彙とし、追加は本書の改訂を要する。

## 6. 非機能要件

- 静的サイト(Jekyll)。自前オリジン(Caddy)から Cloudflare 経由で配信する。JS は推しフィルタ・SP ダイジェスト開閉・タグ索引の絞り込み(`/tags/`)のみで、いずれも JS 無効でも記事本文とタグページには到達できる。
- リポジトリ: `https://github.com/subie-producer/Imas_Daily_News`(初期化済み)を使用。配信 URL は `https://subie-producer.github.io/Imas_Daily_News/` を想定し、Jekyll の baseurl・内部リンク・OGP の絶対 URL はこれを前提に設定する。
- OGP: 記事・号ごとにタイポグラフィベースの OGP 画像をビルド時生成(写真素材不使用)。
- RSS/Atom フィード(号単位・記事単位)。
- メトリクス: 毎実行の candidates 件数・verify 落ち・lint 違反・レビュー往復数・トークンコストを `metrics/` に記録。異常閾値で Discord 通知。
- コスト上限と実行上限(Routine の日次上限)内で 1 日 1 発行+収集数回が収まることを稼働前に実測する。

## 7. 構築ステップ

| Step | 内容 |
|------|------|
| 1 | リポジトリ骨格+スキーマ定義+lint CI(最初から required check) |
| 2 | 確定モックを Jekyll テンプレート+デザイントークンに移植。ダミーデータでビルド確認 |
| 3 | collect 層(sources.yml・Actions cron)稼働開始。創刊までに candidates と stock を蓄積 |
| 4 | compose Routine+校閲チェックリスト+auto-merge+通知。試験号を数回発行して検証 |
| 5 | 創刊(第1号)。以後 append-only で全号保存 |
| 6 | 拡張: もちより面(Discord 取得・破線バッジ)、eval 基盤(発行実績を回帰テスト化) |

## 8. 決定事項と残課題

確定済み: 誕生日マスタは用意済みの JSON を採用(3.3)/24時間ランキングは名鑑ベースのスクリプト算出(3.3)/号数は創刊号=第1号・試験発行=第0号(2.1)/リポジトリは subie-producer/Imas_Daily_News を再利用(第6章)。

残課題:

1. ランキングにおける紙面分とトレンド分の重み——第0号の試験発行で実測して確定する。
2. (α) 表記を外す条件(安定稼働の定義)。
3. もちより面の取得仕様——まず AI 新聞として受け入れられ、安定稼働に至ってから別紙で定義する。